# LINE Chatbot + FastAPI + PostgreSQL (macOS Host)

## 🧩 Setup Instructions

### 1. Clone the repository
```bash
git clone https://github.com/<your-username>/line_Pton.git
cd line_Pton
```
# Build and Run
```bash
docker compose down -v --remove-orphans
docker compose up --build
```
